import { ReactNode } from 'react';

export const metadata = {
  title: 'Evaluación 16PF - KultuRH',
  description: 'Cuestionario de 16 Factores de Personalidad',
};

export default function CandidateLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
